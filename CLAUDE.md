# nextframe-videos

## מה זה

צינור שמכניס לידים נכנסים מווצאפ לגיליון, כדי שסוכן יוכל לקרוא אותם ולנהל
אותם. העסק רץ על מספר WhatsApp Business קיים; היקף: פחות מ-10 לידים ביום.

```
לקוח → Cloud API → Worker (אימות, סינון, dedupe) → Apps Script → Sheet → סוכן
```

## למה כל שכבה קיימת

**אין ל-Cloud API קריאה של הודעות.** יש endpoint אחד, `POST /{id}/messages`,
בלי `GET`. הודעות נכנסות מגיעות ב-push דרך webhook בלבד. לכן שכבת האחסון היא
חובה ולא בחירה — בלי מקבל, אין לידים.

**ה-MCP לא מחליף את זה.** `.mcp.json` מגדיר את WhatsApp Business Tools MCP של
Meta, והוא כלי הקמה: מספרים, תבניות, webhooks. אין בו כלי שקורא שיחות.

## מבנה

```
worker/            Cloudflare Worker — מקבל ה-webhooks
  src/index.js     routing, אימות, dedupe, טיפול בכשלים
  src/signature.js HMAC על הבייטים הגולמיים
  src/parse.js     נרמול הודעת Cloud API → שורת ליד
  src/sinks.js     ← הקובץ היחיד להחליף במעבר לטבלאות אמיתיות
  test/            56 טסטים
apps-script/
  logic.gs         לוגיקה טהורה, נבדקת מ-worker/test
  Code.gs          doPost, גישה לגיליון, נעילה — נבדק מול גלובלים מדומים
docs/              הקמה, מחקר, חוזה הסוכן
```

## פקודות

```bash
cd worker && npm test     # 56 טסטים, מקומי, בלי רשת
cd worker && wrangler deploy
```

הטסטים של `logic.gs` ושל `Code.gs` מריצים את הקבצים **verbatim** דרך
`new Function`, כדי שהכיסוי יהיה על הקוד שנפרס ולא על העתק שלו. אין להעתיק
לוגיקה מהם לטסט — לשנות שם, ולתת לטסט לקרוא.

`doPost` נבדק מול `ContentService`, `SpreadsheetApp` ו-`LockService` מדומים,
עם גיליון מגובה במערך. זה מכסה את שער ה-`TOKEN` ואת החיווט בין קריאת
ה-dedupe לכתיבה — כולל משלוח חוזר שנכתב כבר, המצב שה-KV לא תופס.

## מה הטסטים לא יכולים לתפוס

הטסטים רצים ב-Node, לא ב-workerd. **מגבלות ה-runtime של Workers לא נבדקות
אצלנו.** זה כבר עלה בפועל: `new Response()` ב-global scope עבר 43 טסטים ונדחה
בהעלאה ל-Cloudflare —

> Disallowed operation called within global scope.

גם `wrangler deploy --dry-run` לא תופס את זה. נבדק: הוא אורז מקומית ויוצא 0
על הקוד השבור. הבדיקה קורית בצד השרת.

**לכן הפריסה קודמת לסודות** בשני סקריפטי הפריסה — ההעלאה היא השער היחיד,
ו-Worker בלי סודות הוא בלתי מזיק כי `APP_SECRET` חסר דוחה כל משלוח.

כיסוי אמיתי היה דורש `@cloudflare/vitest-pool-workers`, שמריץ טסטים בתוך
workerd. לא הוסף — תלות נוספת מול באג אחד שנתפס.

## משפחת הכשלים שכל ההגנות נגדה

**המערכת מדווחת הצלחה בזמן שהיא מאבדת נתונים.** כל באג שנמצא בפרויקט הזה היה
מהמשפחה הזאת:

- `doPost` שהחזיר `ok:true` על payload שלא הבין
- dedupe שקרא עמודה קשיחה, ולכן השווה מזהים מול `received_at` אחרי סידור עמודות
- כתיבה שהצליחה עם תשובה שנאבדה, שיצרה שורות כפולות
- פריסה בלי טסטים: Meta מאשרת את ה-Callback URL, מתחילה לשלוח, והלידים נופלים
- `Response` יחיד ב-global scope: גם נדחה ב-runtime, וגם לא ניתן להחזיר פעמיים
  כי גוף תשובה נקרא פעם אחת
- פרסר שהכיר רק את המעטפת של Cloud API: כפתור ה-`Send test` בלוח הבקרה שולח
  `{field, value}` בלי `object`/`entry`/`changes`. הפרסר לא מצא כלום, החזיר
  200, **ו-Meta דיווחה שהבדיקה הצליחה** — בזמן שהשורה לא נכתבה

- `/exec` של Apps Script מחזיר 302, וה-GET שמביא את התשובה נופל לפעמים ב-404
  **אחרי** ש-`doPost` כבר כתב. כשל מדווח על נתונים שנשמרו — הכיוון ההפוך, ובטוח

אף אחד מהם לא מייצר שגיאה. לכן כל ההגנות למטה מעדיפות **רעש על שקט**: ליד
כפול עדיף על ליד אבוד, ו-`warning` עדיף על הנחה שהכל תקין.

## אל תשבור את אלה

**כשל נסגר, לא נפתח.** בלי `APP_SECRET` האימות מחזיר `false`. `TOKEN` שלא
הוחלף גורם ל-`Code.gs` לסרב לכתוב. אין ברירת מחדל שמשתיקה בדיקה.

**שתי שכבות dedupe, ושתיהן נחוצות.** KV ב-Worker חוסם משלוח חוזר שהגיע לפני
העיבוד. קריאת עמודת `wa_message_id` בגיליון חוסמת משלוח חוזר **אחרי** שהשורות
נכתבו — המצב שבו הכתיבה הצליחה אבל התשובה נאבדה, ולכן ה-KV נקי.

**KV מסומן רק אחרי כתיבה מוצלחת.** סימון מוקדם הופך כשל זמני לליד אבוד.

**Apps Script מחזיר 200 גם כשהוא דוחה.** לכן ה-sink בודק `ok` בגוף התשובה,
לא את קוד ה-HTTP. אין להחליף את זה בבדיקת status.

**שתי צורות מעטפת.** `extractChanges` מקבל גם את המעטפת של Cloud API וגם את
`{field, value}` של לוח הבקרה. אין סיכון אבטחה — החתימה כבר אישרה ש-Meta
שלחה. `object` של מוצר אחר עדיין נדחה.

**מפתח חסר ≠ אצווה ריקה.** body בלי `leads`/`messages`/`message` הוא שגיאה;
מערך ריק אמיתי הוא `ok:true, appended:0`. מיזוג שני המצובים מאבד לידים.

## מה פתוח

- ה-MCP **לא מאומת**. דורש סשן אינטראקטיבי, לא עובד מהרשת
- **נפרס ונבדק מול Meta חי** (21.9.2026). מסירה מ-`facebookexternalua` נכתבה
  לגיליון מקצה לקצה. הטסטים עצמם עדיין מקומיים בלבד
- **Coexistence מאומת פעיל** (`is_on_biz_app: true`). המספר בשני העולמות
- האפליקציה **לא מפורסמת**, ולכן מגיעים רק test webhooks מלוח הבקרה. תנועה
  אמיתית דורשת פרסום, שדורש אימות עסק — בבדיקה. המתנה, לא באג
- הגיליון הוא **גיליון בדיקה**. המעבר לטבלאות האמיתיות הוא `sinks.js`

## סגנון

התיעוד והתגובות למשתמש בעברית. קוד, שמות ו-commit messages באנגלית.
